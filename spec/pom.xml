<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>jakarta.agentic-ai</groupId>
        <artifactId>jakarta.agentic-ai-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>jakarta.agentic-ai-spec</artifactId>
    <packaging>pom</packaging>

    <name>Jakarta Agentic AI Specification</name>
    <description>Specification for Jakarta Agentic AI.</description>

    <!--
        Overrides the parent's EPL-2.0: this module builds the specification
        document, which is licensed under the Eclipse Foundation Specification
        License (see src/main/asciidoc/license-efsl.adoc). The module is not
        published, so this is a statement of record rather than repository
        metadata.
    -->
    <licenses>
        <license>
            <name>Eclipse Foundation Specification License (EFSL)</name>
            <url>https://www.eclipse.org/legal/efsl.php</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <properties>
        <maven.build.timestamp.format>MMMM dd, yyyy</maven.build.timestamp.format>
        <revisiondate>${maven.build.timestamp}</revisiondate>
        <maven.deploy.skip>true</maven.deploy.skip>
        <skipPublishing>true</skipPublishing>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.asciidoctor</groupId>
                <artifactId>asciidoctor-maven-plugin</artifactId>
                <dependencies>
                    <dependency>
                        <groupId>org.asciidoctor</groupId>
                        <artifactId>asciidoctorj-pdf</artifactId>
                        <version>2.3.23</version>
                    </dependency>
                </dependencies>
                <executions>
                    <execution>
                        <id>output-html</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>process-asciidoc</goal>
                        </goals>
                        <configuration>
                            <backend>html5</backend>
                            <attributes>
                                <source-highlighter>highlightjs</source-highlighter>
                                <toc/>
                                <linkcss>false</linkcss>
                                <project-version>${project.version}</project-version>
                                <revnumber>${spec.version}</revnumber>
                                <revremark>${spec.status}</revremark>
                                <revdate>${revisiondate}</revdate>
                            </attributes>
                            <outputFile>${project.build.directory}/generated-docs/index.html</outputFile>
                        </configuration>
                    </execution>
                    <execution>
                        <id>output-pdf</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>process-asciidoc</goal>
                        </goals>
                        <configuration>
                            <backend>pdf</backend>
                            <doctype>book</doctype>
                            <templateEngine>slim</templateEngine>
                            <attributes>
                                <pagenums/>
                                <project-version>${project.version}</project-version>
                                <numbered>true</numbered>
                                <docinfo>true</docinfo>
                                <experimental>false</experimental>
                                <toc>true</toc>
                                <revnumber>${spec.version}</revnumber>
                                <revremark>${spec.status}</revremark>
                                <revdate>${revisiondate}</revdate>
                            </attributes>
                        </configuration>
                    </execution>
                    <execution>
                        <id>output-docbook</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>process-asciidoc</goal>
                        </goals>
                        <configuration>
                            <backend>docbook</backend>
                            <doctype>article</doctype>
                            <attributes>
                                <imagesdir>images</imagesdir>
                                <project-version>${project.version}</project-version>
                                <revnumber>${spec.version}</revnumber>
                                <revremark>${spec.status}</revremark>
                                <revdate>${revisiondate}</revdate>
                            </attributes>
                        </configuration>
                    </execution>
                </executions>
                <configuration>
                    <sourceDirectory>src/main/asciidoc</sourceDirectory>
                    <sourceDocumentName>spec.asciidoc</sourceDocumentName>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!--
            The 'stage-release' profile inherited from org.eclipse.ee4j:project
            hardcodes <skip>snapshots</skip> on maven-deploy-plugin, which beats
            the module-level <maven.deploy.skip>true</maven.deploy.skip> property.
            Explicitly override the plugin's <skip> here so this non-API module
            is not staged to Eclipse Nexus (and therefore not promoted to Central).
        -->
        <profile>
            <id>stage-release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-deploy-plugin</artifactId>
                        <configuration>
                            <skip>true</skip>
                        </configuration>
                    </plugin>
                    <!--
                        The parent profile also binds maven-javadoc-plugin:jar
                        on every module. Skip it here so we don't build (and
                        warn about) javadoc for a module that isn't deployed.
                    -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <configuration>
                            <skip>true</skip>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
